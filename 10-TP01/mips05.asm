                    .data
espacioAncii:       .space 20
espacioByte:        .space 20
espacioWord:        .space 80

# la cadena tiene 19 caracteres
# en ascii se utiliza un 2 bits por caracter asi que tengo 38 bits, con space 20 reservo 40 bits.
# sucede lo mismo en byte, el espacio de cada caracter seria de 2 bits.
# en word, el espacio de cada caracter seria de 4 bits, por lo que con space 20 reservo 80 bits.
