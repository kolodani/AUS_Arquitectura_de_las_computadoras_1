            .data
cadena:     .byte   "abcde"        # defino string
octeto:     .byte   0xff

# cuestion 1.14
# al usar la directiva .byte para la cadena de caracteres,
# el simulador da error, porque no puede interpretar el string como un numero